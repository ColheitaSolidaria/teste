@model IEnumerable<ColheitaSolidaria.Models.AprovacaoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cod_adm)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_aprovacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cod_adm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_aprovacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cod_doacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cod_doacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cod_doacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
