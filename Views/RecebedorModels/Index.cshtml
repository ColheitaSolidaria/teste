@model IEnumerable<ColheitaSolidaria.Models.RecebedorModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.n_familiar)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.n_familiar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cod_recebedor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cod_recebedor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cod_recebedor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
